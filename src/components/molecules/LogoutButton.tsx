import React from "react";
import Btn from "../atoms/Btn";

const LogoutButton = () => {
  return (
    <Btn role={`plain`} onClick={() => {}}>
      Logout
    </Btn>
  );
};

export default LogoutButton;
